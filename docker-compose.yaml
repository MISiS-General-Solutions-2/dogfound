version: '3'

services:
  core:
    build:
      context: ./core
      dockerfile: Dockerfile
    ports:
          - "6000:6000"
  ocr:
    build:
      context: ./ocr_mock
      dockerfile: Dockerfile
    ports:
          - "6001:6001"